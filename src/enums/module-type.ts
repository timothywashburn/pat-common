export enum ModuleType {
    AGENDA = 'agenda',
    INBOX = 'inbox',
    TASKS = 'tasks',
    HABITS = 'habits',
    PEOPLE = 'people',
    SETTINGS = 'settings',
    NOTIFICATIONS = 'notifications',
    DEV = 'dev'
}